#!/usr/bin/env php
<?php

require_once realpath(__DIR__) . '/../../../vendor/autoload.php';

$runtime = new \Caffeine\Runtime();
$runtime
    ->bootstrap()
    ->validateEnvironment()
    ->addErrorHandler();

sleep(300);
